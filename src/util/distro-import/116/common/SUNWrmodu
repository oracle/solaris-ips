#
# As there isn't currently a way to delete or rename packages on upgrade,
# create an empty package which will cause the previous contents of the
# package to be deleted when the empty version is installed and declare a
# dependency on the package with the new name.
#
# SUNWrmodu (sparc) was EOFed in snv_113.
#
package SUNWrmodu
$(i386_ONLY)depend SUNWrmod
end package
