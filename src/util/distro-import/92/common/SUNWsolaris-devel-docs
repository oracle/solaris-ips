#
# As there isn't currently a way to delete packages on upgrade, create an
# empty package which will cause the previous contents of the package to
# be deleted when the newer package is installed.
#
# SUNWsolaris-devel-docs was EOFed in snv_92.
#
package SUNWsolaris-devel-docs
consolidation "gnome"
end package
