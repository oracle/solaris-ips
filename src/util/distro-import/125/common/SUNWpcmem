package SUNWpcmem
classification "Drivers/Storage"
import SUNWpcmem

add driver name=pcmem \
    perms="* 0666 bin bin" \
    alias="pccard,memory" \
    alias="pccard1c,40"

add driver name=pcram \
    perms="* 0666 bin bin"
end package
