
pkg
DEPOT FILESYSTEM LAYOUT

$root

$root/trans
	in-flight transactions

$root/metadata/category/pkg_name
	package directory

	release,branch.sequence
		package manifest

	release,latest
		symbolic link to latest manifest

$root/content/
	SHA-1 hash to contents

$root/indices/path/
	pathname components to SHA-1 names

$root/indices/basename/
	basenames only to SHA-1 names

$root/indices/reverse/
	SHA-1 hash to packages, installed path(s)
